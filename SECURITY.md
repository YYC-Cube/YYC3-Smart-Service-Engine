# Security Policy

## 📢 报告安全漏洞

我们非常重视 YYC³ Smart Service Engine 的安全性。如果你发现了安全漏洞，**请不要通过公开的 Issue 提交**。

### 🔒 如何报告安全漏洞

请通过以下方式私密报告安全漏洞：

1. **发送邮件至**: security@yyc3.top
   - 主题格式: `[Security] 漏洞简述`
   - 包含详细信息（见下方模板）

2. **或通过 Gitee 私信联系项目维护者**

### 📋 安全漏洞报告模板

```
**漏洞类型**: (例如: XSS, SQL注入, CSRF, 认证绕过等)
**影响版本**: (受影响的版本号)
**严重程度**: (Critical/High/Medium/Low)
**重现步骤**:
1.
2.
3.

**预期行为**:
**实际行为**:
**可能的修复建议**: (可选)
**联系方式**: (你的邮箱，用于后续沟通)
```

## ⏰ 响应时间线

- **确认收到**: 48 小时内
- **初步评估**: 7 天内
- **修复方案**: 根据严重程度确定
- **公开披露**: 修复发布后

## 🔐 支持的版本

| 版本   | 安全支持状态      |
| ------ | ----------------- |
| v3.x.x | ✅ 当前维护中     |
| v2.x.x | ⚠️ 仅关键安全更新 |
| v1.x.x | ❌ 已停止支持     |

建议始终使用最新版本以获得最佳安全性。

## 🛡️ 安全最佳实践

### 对于部署者

1. **环境变量管理**
   - 不要将敏感信息提交到代码仓库
   - 使用 `.env.local` 存储密钥
   - 定期轮换 API 密钥和令牌

2. **依赖项安全**

   ```bash
   # 定期检查依赖漏洞
   pnpm audit

   # 自动修复可更新的依赖
   pnpm audit fix
   ```

3. **HTTPS 强制使用**
   - 确保生产环境使用 HTTPS
   - 配置 HSTS 头（已默认配置）
   - 使用有效的 SSL 证书

4. **CORS 配置**
   - 仅允许信任的域名
   - 避免使用 `*` 通配符

5. **定期备份**
   - 数据库定期备份
   - 配置文件版本控制
   - 测试恢复流程

### 对于开发者

1. **输入验证**
   - 所有用户输入必须验证和清理
   - 使用 TypeScript 类型系统
   - 服务端再次验证

2. **依赖审计**

   ```bash
   # 安装前检查
   pnpm audit

   # CI/CD 中集成安全检查
   ```

3. **避免常见漏洞**
   - ❌ 不使用 `eval()` 或 `innerHTML`
   - ❌ 不在 URL 中传递敏感数据
   - ✅ 使用 CSP (Content Security Policy)
   - ✅ 实现适当的认证和授权

4. **日志安全**
   - 不要记录密码、token 等敏感信息
   - 使用适当的日志级别
   - 保护日志文件访问权限

## 🔍 已知安全问题

当前没有已知的安全问题。历史安全问题及其修复：

_(暂无)_

## 📊 安全架构概览

YYC³ Smart Service Engine 采用多层安全策略：

```
┌─────────────────────────────────────┐
│         应用层安全                   │
│  • 输入验证与清理                    │
│  • XSS 防护                         │
│  • CSRF Token                       │
├─────────────────────────────────────┤
│         传输层安全                   │
│  • HTTPS/TLS 加密                   │
│  • HSTS 启用                        │
│  • 安全响应头                        │
├─────────────────────────────────────┤
│         基础设施安全                 │
│  • 依赖扫描                         │
│  • 环境隔离                         │
│  • 访问控制                         │
└─────────────────────────────────────┘
```

## 🎖️ 致谢

我们感谢所有负责任地报告安全问题的研究者。所有被确认的安全漏洞报告者将在安全公告中获得致谢（除非您希望保持匿名）。

## 📚 相关资源

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Next.js 安全最佳实践](https://nextjs.org/docs/basic-features/security)
- [Node.js 安全指南](https://nodejs.org/en/docs/guides/security/)
- [CWE (Common Weakness Enumeration)](https://cwe.mitre.org/)

---

**最后更新**: 2026-05-26
